# مراجعة واجهة Archive الجديدة

تمت معاينة `public/index.html` محليًا في 27 أغسطس 2026. ظهرت بطاقة المكتبة الجديدة، عنوان Archive، العداد، البحث، وشريط التنقل السفلي في تخطيط متجاوب. ظهور بعض الصور كمسار غير موجود عند فتح الملف مباشرة عبر `file://` متوقع؛ التطبيق الفعلي يقدّم الأصول من جذر WebView، وسيتم التحقق منها بعد تجهيز `mobile/web-user` وبناء APK.

الهوية الجديدة تستخدم علامة Archive بلا نص داخل الشعار، مع لوحة عاجية وفحمية وكوبالت هادئ. لم تظهر أخطاء JavaScript في فحص السكربتات المضمّنة.

## معاينة Admin

محاولة فتح `public/admin.html` مباشرة عبر `file://` أعادت صفحة تحويل إلى `admin-login.html` غير موجودة ضمن مسار `file://` بسبب المسارات المطلقة، وليس بسبب خلل في HTML. يلزم تقديم الواجهة عبر HTTP محلي أو تشغيل الخادم لاختبارها بصريًا كما ستظهر داخل WebView.

## معاينة Admin عبر HTTP

بعد تجهيز WebView مع Firebase URL العام، ظهرت لوحة Admin الجديدة بصورة صحيحة: شعار Archive الجديد، بطاقة `مركز المحتوى`، أزرار `منشور جديد` و`قسم جديد` و`قائمة جديدة`، الإحصاءات، التبويبات، وقائمة المنشورات القديمة. ظهر تنبيه واضح بأن الوضع الحالي قراءة مباشرة وأن الرفع والحفظ يحتاجان خدمة الإدارة الآمنة. تم تحميل البيانات الفعلية من Firebase: 6 منشورات، 169 صوتًا، و1 PDF ضمن إحصاءات الوسائط، و11 قسمًا.
