# Archive — تدقيق المستودع قبل إعادة البناء

## نطاق التدقيق
تمت مراجعة الخادم، المسارات العامة والإدارية، المصادقة، طبقة الحماية، الرفع والتخزين، كشف أنواع الوسائط، الواجهة العامة، لوحة الإدارة، عامل الخدمة، وصفحة الدخول، ثم تشغيل `npm install` وفحص بنية ملفات JavaScript.

## النتائج الحرجة

1. الحارس الإداري العالمي في `routes/admin.js` معطّل بالتعليق، لذلك مسارات الإدارة قابلة للوصول دون JWT وCSRF.
2. حماية `admin.html` في `server-secure.js` معطّلة بالتعليق، لذلك يمكن فتح لوحة الإدارة مباشرة.
3. اسم المستخدم وهاش كلمة المرور مثبتان داخل `routes/admin-auth.js`، مع تجاهل `ADMIN_USERNAME` و`ADMIN_PASSWORD_HASH` الموجودين في `.env.example`.
4. تسجيل الدخول يطبع اسم المستخدم وطوله وبعض نتائج التحقق في السجل، وهو تسريب غير مناسب لبيانات المصادقة.
5. `dotenv` غير موجود في `package.json` رغم أن الخادم يحاول تحميل `.env`؛ تعليمات التشغيل الحالية قد لا تعمل عند الاعتماد على ملف `.env`.
6. مسار رفع الملفات يعتمد فقط على Cloudinary، بينما واجهة الإدارة تعرض عبارة "بدون حد للحجم" وتضمّن كود FileLu بمفتاح API ثابت داخل المتصفح، وهو مسار غير آمن وغير موثوق ويعتمد على CORS.
7. مسار PDF يستخدم iframe مباشرة أو Google Viewer على الهاتف. هذا يفشل للروابط التي لا تسمح بالوصول العام، ويضيف اعتمادًا خارجيًا لا يمكن ضمانه.
8. بروكسي `/api/stream` لا يفرض مهلة أو حدًا لحجم الاستجابة، ولا يتحقق من البروتوكول بعد إعادة التوجيه؛ يمكن أن يعلق أو يُستغل كـ SSRF/استنزاف موارد.
9. `detectType` و`guessMime` يعتمدان بقوة على امتداد URL، ولذلك تفشل ملفات Cloudinary وArchive.org والروابط التي لا تحتوي امتدادًا أو تعيد `application/octet-stream`.
10. رفع PDF كـ Cloudinary `raw` قد يعيد رابطًا بلا امتداد قابل للاكتشاف، فيُحفظ كنوع غير صحيح أو يُعرض كنص.

## مشاكل منطقية وواجهة

11. `POST /api/admin/anasheed/broadcast-cover` لا يطبق `sectionId` رغم أن الواجهة ترسله وتعرض رسالة توحي بتطبيقه على قسم محدد.
12. ترقيعة وسائط القوائم الجانبية تعتمد على `window.__menuState.sideMenu`، بينما الحالة الحقيقية تستخدم `menuState.items`؛ لذلك وسائط القوائم لا تظهر ولا تعمل.
13. `window.fap` لا يُسند له كائن المشغل، لكن مسار تشغيل وسائط القوائم يستدعي `window.fap`؛ تشغيل الصوت من ذلك المسار يفشل.
14. تنزيل الصوت يستخدم `toast(..., 'success')` بينما الدالة تدعم `error` فقط، لذلك حالة النجاح لا تطبق نمط النجاح.
15. مشغل HLS يقرأ `src` الأصلي بدل الرابط المطبع في بعض الفروع، ولا يحتفظ بمرجع HLS للتنظيف، كما لا يعالج فشل تحميل مكتبة HLS بوضوح.
16. الصور في بعض المواضع تستخدم الرابط المباشر بدل `/api/stream`، ما يسبب فشل CORS أو Content-Type في بعض مزودي التخزين.
17. `admin.html` يحتوي مستمعات وترقيعات متراكمة تستبدل دوال وأزرار بعد تعريفها، ما يزيد احتمال التكرار والسلوك غير المتوقع.
18. `admin.html` لا يعرض حقول metadata كاملة للوسائط رغم أن الخادم يدعمها، ولا يتحقق من الحجم/النوع قبل الرفع.
19. `window.__lastUploadedMime` لا يتم ضبطه بشكل موثوق لمسارات الرفع التي تستدعي المرجع الأصلي داخل الإغلاق، لذلك بعض سجلات الأناشيد تحفظ MIME فارغًا.
20. عامل الخدمة يضع `/js/api.js` في قائمة precache رغم عدم وجود الملف، ويستخدم هوية `atheer-v4.2` وقد يفشل تثبيته أو يحتفظ بإصدار قديم.
21. مفاتيح التخزين المحلي للجلسة ومواضع الفيديو تحمل هوية Atheer القديمة، كما أن admin login وadmin page يختلفان في مكان حفظ access token.
22. بعض الروابط المضمنة تعتمد على `iframe` من منصات خارجية وسياسة CSP الحالية، دون رسائل بديلة واضحة عند منع التضمين.

## حالة الفحص

- `npm install --no-audit --no-fund`: ناجح.
- `node --check` لجميع ملفات JavaScript: ناجح.
- لا توجد اختبارات آلية أو بنية frontend منفصلة؛ التطبيق عبارة عن Express مع صفحات HTML كبيرة inline.

## خطة الإصلاح المعتمدة

أولًا تفعيل حراسة الإدارة وقراءة بيانات الحساب من البيئة وإضافة `dotenv` وتنظيف السجلات. ثانيًا إعادة تصميم نموذج الوسائط بتمييز صريح للنوع وMIME والاسم، وإضافة رفع server-side آمن بحدود وحفظ PDF كملف قابل للعرض. ثالثًا جعل بروكسي الوسائط أكثر أمانًا ودعمًا للـ Range وHEAD والمهلات. رابعًا إصلاح منطق الروابط والمنصات ووسائط القوائم الجانبية. خامسًا إعادة تسمية Archive وتحديث الهوية البصرية وعامل الخدمة. سادسًا إضافة اختبارات عملية لمسارات الصحة والكشف والبروكسي والتحقق من الحماية، ثم تحديث README والتقرير.

## تحقق مرئي بعد التعديل

تم فتح الواجهة محليًا في Chromium بنجاح. ظهر عنوان الصفحة `Archive — منصة وسائط`، وظهر اسم Archive في الشعار والبحث والقائمة الجانبية، كما فتحت القائمة الجانبية دون أخطاء في الحالة الفارغة. لا توجد منشورات لأن Firebase غير مهيأ في بيئة الاختبار، وهذا متوقع.

## تحقق App Shell في المتصفح

تم تشغيل Archive محليًا وفتح الواجهة في Chromium. ظهر اسم Archive والواجهة نفسها دون رابط خارجي، وكان `manifest.webmanifest` مرتبطًا من الصفحة، كما كان Service Worker مدعومًا ومتحكمًا بالصفحة (`serviceWorkerController: true`). وضع `standalone` لا يظهر أثناء اختبار المتصفح العادي لأنه لا يفتح كتطبيق مثبت، لكنه مضبوط في manifest.

## فحص أصول Android

تم إنشاء وفحص `mobile/branding/splash.png` و`mobile/branding/icon.png`. شاشة البداية تحمل Archive وMEDIA ARCHIVE بتدرجات البنفسجي والفيروزي والبرتقالي، والأيقونة مربعة عالية الدقة بخطوط أرشيفية بيضاء على خلفية ملونة. لا تحتوي الأصول على أسرار أو بيانات مستخدم.
